# Runtime dependencies, pinned to exact versions so a build is reproducible and a
# new release cannot arrive unreviewed. To upgrade: bump a pin here, then let the
# tests and pip-audit (both run in CI) vouch for the result. Last reviewed
# 2026-09-05.
fastapi==0.141.1
uvicorn[standard]==0.52.4
SQLAlchemy==2.0.52
PyMySQL==1.2.0
pydantic==2.13.5
jinja2==3.1.6
PyYAML==6.0.3
python-multipart==0.0.32
reportlab==5.0.1
segno==1.6.6
alembic==1.19.2
itsdangerous==2.2.0
httpx==0.28.1
Pillow==12.3.0
