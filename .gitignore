# Build output. Now built from the REST API on a LAN box and committed here by
# retro-hardware-db-2's tools/publish.sh, so the deploy workflow can serve it.

# Generated label PDFs (regenerated by make_labels.py)
/labels/*.pdf

# theretroweb debug HTML dumps (--dump-html)
/labels/*.html

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS cruft
.DS_Store
Thumbs.db
